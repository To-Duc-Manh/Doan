<%- include('headerUser.ejs') %>
  <div class="container" style="margin-top: 50px; margin-bottom: 150px">
    <div class="row">
      <div class="col-lg-3">
        <div class="link_order">
          <div class="user_order">
            <p>
              <%= user.ho_ten %>
            </p>
          </div>
          <ul class="">
            <li class="item_"><a href="/tai_khoan">Tài khoản của tôi</a></li>
            <li class="item_ mt-3"><a href="/order">Dơn mua hàng</a></li>
            <li class="item_ mt-3"><a href="/bao_hanh">Đơn bảo hành</a></li>
            <li class="item_ mt-3 pb-3"><a href="/lich_su_mua_hang">Lịch sử mua hàng</a></li>
          </ul>
        </div>
      </div>
      <div class="col-lg-9">
        <ul class="nav nav-tabs">
          <li class="nav-item">
            <a class="nav-link active" href="#cho_xac_nhan" data-toggle="tab">Chờ xác nhận</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#da_xac_nhan" data-toggle="tab">Đã xác nhận</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#dang_giao" data-toggle="tab">Đang giao hàng</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#da_giao" data-toggle="tab">Đã giao</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#thanh_cong" data-toggle="tab">Thành công</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#da_huy" data-toggle="tab">Đã hủy</a>
          </li>
        </ul>

        <div class="tab-content mt-1">
          <div class="tab-pane active link_order" id="cho_xac_nhan">
            <div class="">
              <% if (don_mua_hang.length> 0) {
                var hasActiveOrder = false;
                don_mua_hang.forEach(function(don_mua_hang_item, index) {
                if (don_mua_hang_item.trang_thai === 1) {
                hasActiveOrder = true;
                %>
                <div class="row">
                  <div class="">
                    <p class="user_order text-start">HD: <%= don_mua_hang_item.id %>
                    </p>
                  </div>
                  <% var hasActiveProducts=false; if (data && data.length> 0) {
                    data.forEach(function(order, index) {
                    if (user.id == order.khach_hang_id && order.don_mua_hang_id === don_mua_hang_item.id) {
                    hasActiveProducts = true;
                    %>
                    <div class="row">
                      <div class="col-lg-3">
                        <img style="width: 150px; height: 100px;" class="img-thumbnail"
                          src="/imgUpload/<%= order.hinh_anh %>">
                      </div>
                      <div class="col-lg-6">
                        <p>Thông tin sản phẩm</p>
                        <p>
                          <%= order.ten_san_pham %> - <%= order.mau_sac %> - <%= order.dung_luong %> - x<%=
                                  order.so_luong %>
                        </p>
                      </div>
                      <div class="col-lg-3">
                        <p>Giá</p>
                        <p>
                          <%= order.gia_ban %>
                        </p>

                      </div>
                    </div>
                    <% } }); } if (!hasActiveProducts) { %>
                      <p>Không có dữ liệu</p>
                      <% } %>
                        <div>
                          <p class="text-end mx-2">Tổng tiền phải thanh toán: <%= don_mua_hang_item.tong_tien_thanh_toan
                              %>
                          </p>
                        </div>
                        <div class="text-end">
                          <a href="#" onclick="confirmCancelOrder(`<%= don_mua_hang_item.id %>`)"
                            class="btn btn-outline-danger mx-2 mb-3">Hủy đơn hàng</a>
                        </div>
                </div>
                <% } }); if (!hasActiveOrder) { %>
                  <p>Không có dữ liệu</p>
                  <% } } else { %>
                    <p>Không có dữ liệu</p>
                    <% } %>
            </div>
          </div>
          <div class="tab-pane" id="da_xac_nhan">
            <% if (don_mua_hang.length> 0) {
              var hasActiveOrder = false;
              don_mua_hang.forEach(function(don_mua_hang_item, index) {
              if (don_mua_hang_item.trang_thai === 2) {
              hasActiveOrder = true;
              %>
              <div class="row">
                <div class="">
                  <p class="user_order text-start">HD: <%= don_mua_hang_item.id %>
                  </p>
                </div>
                <% var hasActiveProducts=false; if (data && data.length> 0) {
                  data.forEach(function(order, index) {
                  if (user.id == order.khach_hang_id && order.don_mua_hang_id === don_mua_hang_item.id) {
                  hasActiveProducts = true;
                  %>
                  <div class="row">
                    <div class="col-lg-3">
                      <img style="width: 150px; height: 100px;" class="img-thumbnail"
                        src="/imgUpload/<%= order.hinh_anh %>">
                    </div>
                    <div class="col-lg-6">
                      <p>Thông tin sản phẩm</p>
                      <p>
                        <%= order.ten_san_pham %> - <%= order.mau_sac %> - <%= order.dung_luong %> - x<%= order.so_luong
                                %>
                      </p>
                    </div>
                    <div class="col-lg-3">
                      <p>Giá</p>
                      <p>
                        <%= order.gia_ban %>
                      </p>

                    </div>
                  </div>
                  <% } }); } if (!hasActiveProducts) { %>
                    <p>Không có dữ liệu</p>
                    <% } %>
                      <p class="text-end">Tổng tiền phải thanh toán: <%= don_mua_hang_item.tong_tien_thanh_toan %>
                      </p>
              </div>
              <% } }); if (!hasActiveOrder) { %>
                <p>Không có dữ liệu</p>
                <% } } else { %>
                  <p>Không có dữ liệu</p>
                  <% } %>


          </div>
          <div class="tab-pane" id="dang_giao">
            <% if (don_mua_hang.length> 0) {
              var hasActiveOrder = false;
              don_mua_hang.forEach(function(don_mua_hang_item, index) {
              if (don_mua_hang_item.trang_thai === 3) {
              hasActiveOrder = true;
              %>
              <div class="row">
                <div class="">
                  <p class="user_order text-start">HD: <%= don_mua_hang_item.id %>
                  </p>
                </div>
                <% var hasActiveProducts=false; if (data && data.length> 0) {
                  data.forEach(function(order, index) {
                  if (user.id == order.khach_hang_id && order.don_mua_hang_id === don_mua_hang_item.id) {
                  hasActiveProducts = true;
                  %>
                  <div class="row">
                    <div class="col-lg-3">
                      <img style="width: 150px; height: 100px;" class="img-thumbnail"
                        src="/imgUpload/<%= order.hinh_anh %>">
                    </div>
                    <div class="col-lg-6">
                      <p>Thông tin sản phẩm</p>
                      <p>
                        <%= order.ten_san_pham %> - <%= order.mau_sac %> - <%= order.dung_luong %> - x<%= order.so_luong
                                %>
                      </p>
                    </div>
                    <div class="col-lg-3">
                      <p>Giá</p>
                      <p>
                        <%= order.gia_ban %>
                      </p>

                    </div>
                  </div>
                  <% } }); } if (!hasActiveProducts) { %>
                    <p>Không có dữ liệu</p>
                    <% } %>
                      <p class="text-end">Tổng tiền phải thanh toán: <%= don_mua_hang_item.tong_tien_thanh_toan %>
                      </p>
              </div>
              <% } }); if (!hasActiveOrder) { %>
                <p>Không có dữ liệu</p>
                <% } } else { %>
                  <p>Không có dữ liệu</p>
                  <% } %>
          </div>
          <div class="tab-pane" id="da_giao">
            <% if (don_mua_hang.length> 0) {
              var hasActiveOrder = false;
              don_mua_hang.forEach(function(don_mua_hang_item, index) {
              if (don_mua_hang_item.trang_thai === 4) {
              hasActiveOrder = true;
              %>
              <div class="row">
                <div class="">
                  <p class="user_order text-start">HD: <%= don_mua_hang_item.id %>
                  </p>
                </div>
                <% var hasActiveProducts=false; if (data && data.length> 0) {
                  data.forEach(function(order, index) {
                  if (user.id == order.khach_hang_id && order.don_mua_hang_id === don_mua_hang_item.id) {
                  hasActiveProducts = true;
                  %>
                  <div class="row">
                    <div class="col-lg-3">
                      <img style="width: 150px; height: 100px;" class="img-thumbnail"
                        src="/imgUpload/<%= order.hinh_anh %>">
                    </div>
                    <div class="col-lg-6">
                      <p>Thông tin sản phẩm</p>
                      <p>
                        <%= order.ten_san_pham %> - <%= order.mau_sac %> - <%= order.dung_luong %> - x<%= order.so_luong
                                %>
                      </p>
                    </div>
                    <div class="col-lg-3">
                      <p>Giá</p>
                      <p>
                        <%= order.gia_ban %>
                      </p>

                    </div>
                  </div>
                  <% } }); } if (!hasActiveProducts) { %>
                    <p>Không có dữ liệu</p>
                    <% } %>
                      <p class="text-end">Tổng tiền thanh toán: <%= don_mua_hang_item.tong_tien_thanh_toan %>
                      </p>
                      <div class="text-end">
                        <a onclick="da_nhan_hang(`<%= don_mua_hang_item.id %>`)"
                          class="btn btn-outline-danger mx-2 mb-3">Đã nhận hàng</a>
                      </div>
              </div>
              <% } }); if (!hasActiveOrder) { %>
                <p>Không có dữ liệu</p>
                <% } } else { %>
                  <p>Không có dữ liệu</p>
                  <% } %>
          </div>
          <div class="tab-pane" id="thanh_cong">
            <% if (don_mua_hang.length> 0) {
              var hasActiveOrder = false;
              don_mua_hang.forEach(function(don_mua_hang_item, index) {
              if (don_mua_hang_item.trang_thai === 6) {
              hasActiveOrder = true;
              %>
              <div class="row">
                <div class="">
                  <p class="user_order text-start">HD: <%= don_mua_hang_item.id %>
                  </p>
                </div>
                <% var hasActiveProducts=false; if (data && data.length> 0) {
                  data.forEach(function(order, index) {
                  if (user.id == order.khach_hang_id && order.don_mua_hang_id === don_mua_hang_item.id) {
                  hasActiveProducts = true;
                  %>
                  <div class="row">
                    <div class="col-lg-3">
                      <img style="width: 150px; height: 100px;" class="img-thumbnail"
                        src="/imgUpload/<%= order.hinh_anh %>">
                    </div>
                    <div class="col-lg-6">
                      <p>Thông tin sản phẩm</p>
                      <p>
                        <%= order.ten_san_pham %> - <%= order.mau_sac %> - <%= order.dung_luong %> - x<%= order.so_luong
                                %>
                      </p>
                    </div>
                    <div class="col-lg-3">
                      <p>Giá</p>
                      <p>
                        <%= order.gia_ban %>
                      </p>
                      <div class="text-end">
                        <button type="button" class="btn btn-outline-primary mx-1" data-bs-toggle="modal"
                          data-bs-target="#staticBackdrop<%= order.id %>">
                          Bảo hành
                        </button>

                        <div class="modal fade " id="staticBackdrop<%= order.id %>" data-bs-backdrop="static"
                          data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel"
                          aria-hidden="true">
                          <div class="modal-dialog modal-dialog-scrollable">
                            <div class="modal-content">
                              <div class="modal-header">
                                <h5 class="modal-title" id="staticBackdropLabel">Bảo hành sản phẩm</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"
                                  aria-label="Close"></button>
                              </div>
                              <div class="modal-body">
                                <form>
                                  <div class="">
                                    <label for="" class="form-label">Tên sản phẩm</label>
                                    <input type="text" class="form-control" id="" name=""
                                      value="<%= order.ten_san_pham %>" required readonly>
                                  </div>
                                  <div class="">
                                    <label for="validationCustom02" class="form-label">Ghi chú</label>
                                    <textarea class="form-control" name="ghi_chu_bh<%= order.ten_san_pham %>"></textarea>
                                  </div>
                                </form>
                              </div>
                              <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                <button type="button"
                                  onclick="luuPhieuBaoHanh('<%= order.id %>', document.querySelector('[name=ghi_chu_bh<%= order.ten_san_pham %>]').value)"
                                  class="btn btn-primary">Gửi</button>
                              </div>
                            </div>
                          </div>
                        </div>

                      </div>
                    </div>
                  </div>
                  <% } }); } if (!hasActiveProducts) { %>
                    <p>Không có dữ liệu</p>
                    <% } %>
                      <p class="text-end">Tổng tiền thanh toán: <%= don_mua_hang_item.tong_tien_thanh_toan %>
                      </p>
              </div>
              <% } }); if (!hasActiveOrder) { %>
                <p>Không có dữ liệu</p>
                <% } } else { %>
                  <p>Không có dữ liệu</p>
                  <% } %>
          </div>
          <div class="tab-pane" id="da_huy">
            <% if (don_mua_hang.length> 0) {
              var hasActiveOrder = false;
              don_mua_hang.forEach(function(don_mua_hang_item, index) {
              if (don_mua_hang_item.trang_thai === 5) {
              hasActiveOrder = true;
              %>
              <div class="row">
                <div class="">
                  <p class="user_order text-start">HD: <%= don_mua_hang_item.id %>
                  </p>
                </div>
                <% var hasActiveProducts=false; if (data && data.length> 0) {
                  data.forEach(function(order, index) {
                  if (user.id == order.khach_hang_id && order.don_mua_hang_id === don_mua_hang_item.id) {
                  hasActiveProducts = true;
                  %>
                  <div class="row">
                    <div class="col-lg-3">
                      <img style="width: 150px; height: 100px;" class="img-thumbnail"
                        src="/imgUpload/<%= order.hinh_anh %>">
                    </div>
                    <div class="col-lg-6">
                      <p>Thông tin sản phẩm</p>
                      <p>
                        <%= order.ten_san_pham %> - <%= order.mau_sac %> - <%= order.dung_luong %> - x<%= order.so_luong
                                %>
                      </p>
                    </div>
                    <div class="col-lg-3">
                      <p>Giá</p>
                      <p>
                        <%= order.gia_ban %>
                      </p>

                    </div>
                  </div>
                  <% } }); } if (!hasActiveProducts) { %>
                    <p>Không có dữ liệu</p>
                    <% } %>
                      <div>
                        <p class="text-end mx-2">Tổng tiền thanh toán: <%= don_mua_hang_item.tong_tien_thanh_toan %>
                        </p>

                      </div>
                      <div class="text-end">
                        <a href="#" onclick="mua_lai_hang(`<%= don_mua_hang_item.id %>`)"
                          class="btn btn-outline-success mx-2 mb-3">Mua lại</a>
                      </div>
              </div>
              <% } }); if (!hasActiveOrder) { %>
                <p>Không có dữ liệu</p>
                <% } } else { %>
                  <p>Không có dữ liệu</p>
                  <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>



  <script>
    function confirmCancelOrder(orderId) {
      if (confirm("Bạn có chắc chắn muốn hủy đơn hàng này không?")) {
        // Nếu người dùng đồng ý, gửi yêu cầu hủy đơn hàng đến máy chủ
        window.location.href = "/order_huy/" + orderId;

      } else {
        // Nếu người dùng không đồng ý, không thực hiện hành động nào
        return false;
      }
    }
    function da_nhan_hang(orderId) {
      if (confirm("Đã nhận hàng")) {
        window.location.href = "/da_nhan_hang/" + orderId;
      } else {
        // Nếu người dùng không đồng ý, không thực hiện hành động nào
        return false;
      }
    }
    function mua_lai_hang(orderId) {
      window.location.href = "/mua_lai_hang/" + orderId;
    }

    function luuPhieuBaoHanh(orderId, ghiChu) {
      fetch('/luu_phieu_bao_hanh', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ orderId, ghiChu }),
      })
        .then(response => response.json())
        .then(data => {
          alert(data.message);
          window.location.href = '/bao_hanh';
        })
        .catch((error) => {
          console.error('Error:', error);
          alert('Đã có lỗi xảy ra');
        });
    }

  </script>
  <%- include('footerUser.ejs') %>